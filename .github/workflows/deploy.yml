name: Deploy Website to DreamHost

on:
  push:
    branches:
      - main  # Vérifie si ta branche principale s'appelle "main" ou "master"

jobs:
  web-deploy:
    name: 🎉 Deploy
    runs-on: ubuntu-latest
    steps:
    - name: 🚚 Get latest code
      uses: actions/checkout@v4

    - name: 📂 Sync files
      uses: SamKirkland/FTP-Deploy-Action@v4.3.5
      with:
        server: ${{ secrets.FTP_SERVER }}
        username: ${{ secrets.FTP_USERNAME }}
        password: ${{ secrets.FTP_PASSWORD }}
        # ⚠️ ATTENTION : C'est la partie la plus importante ci-dessous
        server-dir: /home/dh_33tuig/aleoss2.dreamhosters.com/
        exclude: |
          **/.git*
          **/.github*
          README.md
